#ifndef __KLT_TRACKER_H__
#define __KLT_TRACKER_H__

#include <Eigen/Dense>
#include <opencv2/opencv.hpp>

class KLTTracker {
  private:
    /* data */
  public:
    KLTTracker(/* args */);
    ~KLTTracker();
};

#endif